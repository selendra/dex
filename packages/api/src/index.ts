/**
 * DEX API - TypeScript Entry Point
 * 
 * This is the main entry point for the DEX REST API server.
 * Run with: npm run dev (development) or npm start (production)
 */

import { startServer } from './server';

// Start the server
startServer();
